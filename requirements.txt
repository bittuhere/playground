# Optional dependency used by the protected /admin dashboard to read hosted Firebase data.
# The app remains dependency-free when FIREBASE_SERVICE_ACCOUNT_JSON is not configured.
firebase-admin>=6.5,<8
